name: Building
on: [push]
jobs:
  building:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python
        with:
          python-version: '3.10'
      - name: Install Poetry
        uses: snok/install-poetry@v1